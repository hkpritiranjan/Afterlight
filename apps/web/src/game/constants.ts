export const TILE_SIZE = 32;
export const MAP_TILES_X = 120;
export const MAP_TILES_Y = 90;
export const MAP_WIDTH = TILE_SIZE * MAP_TILES_X;   // 3840 px
export const MAP_HEIGHT = TILE_SIZE * MAP_TILES_Y;  // 2880 px

export const PLAYER_SPEED = 180;       // px / second
export const PLAYER_RADIUS = 8;        // px
export const INTERACTION_RADIUS = 70;  // px — how close to trigger prompt
